# Production Environment Variables
# These will be set in your hosting platform (Heroku, Netlify, etc.)

# MongoDB Atlas URI (set in Heroku config)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/prompt-optimizer?retryWrites=true&w=majority

# JWT Secret (set in Heroku config)
# JWT_SECRET=your-super-secure-jwt-secret-here

# Server Configuration (set in Heroku config)
# PORT=5001
# NODE_ENV=production

# Frontend URL (set in Heroku config)
# FRONTEND_URL=https://your-app-name.netlify.app

# React App API URL (set in Netlify config)
# REACT_APP_API_URL=https://your-app-name-backend.herokuapp.com/api
